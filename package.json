{
  "scripts": {
    "test": "mocha  --timeout 50000  --reporter mocha-allure-reporter",
    "report": "allure generate allure-results --clean -o allure-report && allure open allure-report"
  },
  "dependencies": {
    "mocha": "^7.1.2",
    "mocha-allure-reporter": "^1.4.0",
    "puppeteer": "^3.0.4"
  }
}
